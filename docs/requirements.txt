# Documentation build only — the course itself needs ../requirements.txt
sphinx>=7.2
myst-nb>=1.0          # Markdown sources + notebook rendering (no pandoc needed)
linkify-it-py>=2.0    # MyST "linkify" extension: bare URLs become links
furo>=2024.1.29       # theme
sphinx-design>=0.5    # grids, cards, tab-sets
sphinx-copybutton>=0.5

# Optional, for `sphinx-autobuild docs docs/_build/html`:
# sphinx-autobuild>=2021.3.14
